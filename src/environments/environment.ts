/**
 * Development defaults (used by `ng serve` and any non-production build
 * configuration). Swapped for environment.prod.ts via the `fileReplacements`
 * entry in angular.json's production configuration.
 */
export const environment = {
  apiBaseUrl: 'http://localhost:8000',
};
